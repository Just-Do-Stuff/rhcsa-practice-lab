Task 1 — Configure Firewall Settings

Oh look it's us configuring firewalld again =).

## Instructions

1. Verify the status of `firewalld`.
2. Start and enable the firewall service.
3. Allow a service through the firewall permanently.
4. Allow a specific port through the firewall permanently.
5. Reload the firewall configuration.
6. Verify the firewall rules.
